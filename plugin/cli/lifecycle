#!/usr/bin/env python3
"""The `lifecycle` entry point. One command, one fire log."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from lifecycle_core.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
