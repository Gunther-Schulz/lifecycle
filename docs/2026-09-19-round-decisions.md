# The design round's decisions — grounds (2026-09-19, round desk)

**The ledger carries each decision and points here; this file is the body,
never a second copy of any decision. Map: `docs/2026-09-19-round-map.md`.
Pattern: `docs/head-rule-decision-2026-09-19.md`. Declines are first-class.**

## D-1 — lc-234's sampling plan

Sample set: for each calendar day with at least one commit changing
`len(refusals.ROWS)`, that day's LAST such commit (end-of-day roster state —
post-repair, so mid-day transient reds do not read as erosion); if more than
ten, thin keeping first, last, and every second interior sample; HEAD always
included. Execution: one detached worktree per sampled commit (prove-rows
refuses a dirty tree; the technique is proven — lc-234's own evidence cites
the capture_dominated single-ident run), worktree REMOVED after its sample
(lc-201's class: no registration left behind), one at a time, the live tree
untouched. Join: per-row verdicts keyed on row ident across samples; a row
ident absent at a later sample is COULD-NOT-TRACK (renames read as
died-plus-born and must not fold into regressed — the probe's own third
answer). Riders 1-2 of the design doc bind unchanged: COULD NOT VERIFY
(arrangement broke) reported apart from FAILED (mutation no longer darkens);
reach = roster channel only. Verdict pre-registered in lc-234's
done-criterion, unmoved. Route: brief-covered execution once specced —
dispatches; the grading of the result stays at this desk, and the result
gates D-5's always-on admissions before any of them deploy.

## D-2 — the arc is a NEW REGISTERED KIND, not a widened lane

The unification read (arc-walk requirement 6, executed): lanes are STATELESS
router rows — declaration entries whose `Trigger:` a `lane list` run
evaluates, pointing at runbooks; no per-instance persisted state, no stages
(read at lanes.py / the router section of CLAUDE.md). An arc is a STATEFUL
CARRIER entry: goal, current stage, narrowing, premises (the E12 inbox),
yield count, belief lines with bases and kill-conditions. Widening lanes to
carry that rebuilds the item carrier inside the router — two homes for one
shape. What IS reused, by design rather than concession: stage exits
evaluate through `lanes.evaluate_trigger` (the one-evaluator law; the
operator-judged arm is the existing `none, declared why` shape); dated
deadlines get their OBSERVER as a date-predicate LANE (walk 4's requirement,
met by the mechanism that already exists); entry/close ride the two-exits
and conservation discipline like items. Multi-arc ordering (head-rule
re-open (d)) is settled INSIDE lc-231's design, not by a grade — the
head-rule decision's reach note stands. lc-231's full design is this round's
step-3 work, attacked before build.

## D-3 — the schema-level third answer: a SCOPED general mechanism

**Admitted: the REGISTERED-CLOSED-VOCABULARY contract.** Every closed
vocabulary the declaration or the carriers' slots carry is REGISTERED (name,
members, the consumer that renders it), and each carries a typed
OUT-OF-VOCABULARY arm — "cannot-express: <reason>" — whose rendering is
DISTINCT from every member (never folded into a neighbour). A registered
vocabulary without the arm is a checker finding (the kind_stage_undeclared
pattern one level up). A recorded OOV instance WITH ITS REASON is the
WIDENING SIGNAL: new members are minted from recorded reasons, never
guessed — which is the Begehung's sharpened class ("the repair is where the
class survives") turned into the mint mechanism.

**Graded against the population, member by member:** dissolved or surfaced
by the contract — (a) foreign reader, (b)+rewrite's three unmarked-commit
causes, (c) untestable waits (see D-8: enough recorded instances to mint a
REAL member directly), (j) never-run vs runs-quiet, (l) the lock's cross-repo
state, f1 malformed-when (the first red-first input: MALFORMED is an OOV
state, never UNDECLARED), f4 the census bucket (second red-first input,
D-7), E10's shelf-life evidence (a slot-kind gap the contract registers).
NOT forced under it, routed to their own classes: (d) slot-format collision
(lc-233, booked), (e) grade-derivation default (law 26 class), (f)/(g)/(k)
instrument+detector+verb gaps (lc-173, lc-238, finding (k)), (h) declared
exemption (law 11 route), (i) claim-beside-mechanism (law 26), f5 pattern
sibling sweep. **(m) is the mechanism's NAMED NON-GOAL**: two correct
mechanisms with no shared exit are not a rendering defect; each such
conflict needs its own shared-exit design (first specimen: the
redaction-verb / scan-at-admission pair the directive records).

**Stop/yield, per the admission lens:** vocabularies register ON CONTACT
(when a gap is found or a vocabulary is next touched), never by a big-bang
sweep — the sweep would be the Collector's Fallacy arm. Yield = checker
findings plus recorded OOV instances; a quarter with zero OOV recordings
across all registered vocabularies is the signal the contract is either
complete or unread, and WHICH is the fire-rate review's question.

## D-4 — lc-236's token home: the waves VERB'S OUTPUT

The route line quotes the verb; a prose convention unfed by a mechanism
under-fires (this arc's own measured pattern, the adherence split). Relation
to lc-173/(f): a collapsed join makes any consolidation token confidently
wrong, so lc-236 is blocked-by lc-173 (an ITEM blocker — expressible), and
(f)'s repair shape — the join answering COULD-NOT-DISCRIMINATE when one
shared entry covers most of the schedulable set, instead of "one lane" —
folds into lc-173's design as its instrument-level third answer.

## D-5 — admission of the MISSING edges

**Build this arc** (each with stop/yield at its design): the D-3 contract
(above); f2's wiring of read_moments — the gap books NOW, the always-on
deployment gates on lc-234's probe result (blocker type: item, lc-234);
E10 shelf-life slot marker (`re-derive-at-read: <command>` on evidence-class
slots), consumed by mechanism 5's existing pickup seam; D-7's census bucket;
f5's _RELAY widening WITH the sibling-sweep duty discharged in the same
change; D-9's trailer default (lc-52 direction below). **Fold:** E12
(premise inbox) and E13 (compiled assumptions) into the arc kind's design —
premises with visible emptiness and reopen-propagation are lc-231's own
entry inputs; a tool-side assumption slot is the CachyOS pilot's to
exercise (beat 1 kink harvest). **Decline:** E3 (runbook-mint seam) —
duplicate home: it is lc-97's close-ceremony design input, recorded there,
with the Cynefin bound attached; E4 (probe-used-twice observer) — no
computable trigger, mechanism bar unmet, corpus rule + operator backstop
stand. **Park:** E11 (done-where-read as a general mechanism) — no
computable trigger identified beyond what lc-111 already books;
re-open event: the arc kind's completion vocabulary landing, or the next
measured built-not-live instance after f2's. E17 stays as lc-240 books it
(parked on this round's close, by construction).

## D-6 — the map as declaration data: BOOKED, not built this arc

Edges reference kinds and seams that D-2 and D-3 are about to move; a data
model cut before the arc kind and the vocabulary contract land is rework by
construction. Blocked-by: the arc kind's build. Design input: the map file
plus frame point 4.

## D-7 — census third bucket: YES

"PREDATES THE MECHANISM": the count line splits into three, zero new
findings — lc-175's MUST-NOT-MOVE honoured exactly. Basis: Begehung f4's
executed evidence and the census docstring's own three-answer insistence.
D-3's second red-first input. [CORRECTED IN PLACE 2026-09-19, twice, per
the attack rounds: the date-epoch mechanism was replaced by the
forward-only door stamp (ledger, D-7 revision), and the "all eight
blockers" figure this section once carried went stale the same day it was
written — lc-52's re-park moved the population to nine ten minutes before
the v2 lock cited eight (attack r2 B1, the persisted-count class). The
population figure is DERIVED from `blocker_slot_census` at any consuming
moment, never restated here or in any design.]

## D-8 — blocker type for untestable events: mint the REAL member

Type `external <event>`: evaluated by nothing, cleared by hand or desk with
the event named. Six recorded specimens (the four cache-fix waits, lc-235,
lc-236) clear D-3's own widening bar — recorded instances, never guessed
members. Specimens re-typed at the contract's build.

## D-9 — lc-52's default: the plugin supplies its own

Default source: the desk-state file (an EXISTING registered kind,
`$XDG_STATE_HOME/lifecycle/desk-state/`) written by the plugin's own
session-start machinery; `LIFECYCLE_COMMIT_TRAILER` overrides;
unmarked-with-warning stays the honest fallback when neither exists. Neither
of the blocker's two named options survives whole: a dotfiles export is a
cross-repo duty, and raw harness env is a shape this repo does not own — the
plugin owning BOTH ends (hook writes, verb reads) is the pit-of-success
form. Detail design at the item's build; lc-52 unparks with this direction.

## D-10 — lc-237: premise re-grounded, disposition folded into D-3

Evidence amended to HEAD facts (four evaluate_trigger call sites, one in
declaration.py:1624 via read_moments, landed by lc-224 — the Begehung's f3).
The "retire the predicate mode until something evaluates it" repair is DEAD:
something now evaluates it, for readers. Surviving repair direction: the
trigger-status vocabulary distinguishes never-run from runs-quiet — a D-3
contract instance; lc-237 re-points blocked-by to the D-3 item at its
booking.

## Settled and relayed (recorded elsewhere, listed for the round's record)

lc-239 two-beat staging (operator first-hand, ledger 2026-09-19). Head-rule:
declined both ways, only re-open (d) alive, inside D-2. pbs-office
follow-on: rides the close digest as addendum 9 states it — federated
declaration, never content migration; the operator's own
working-but-not-final grade carried, not endorsed.
