# Begehung MAP — the lc-157 design (five mechanisms for a self-building groove)

System under review: the DESIGN, not the lifecycle tool. Design of record:
`docs/answerable-not-felt.md` (the PLAN section). Supporting measurements:
`docs/audits/2026-09-18-capture-routes-and-the-groove.md` and
`docs/audits/2026-09-18-desk-peer-catch-study.md`.

Interval: a row unvisited for the two rounds before the current one is itself
a finding at the next invocation.

Surfaces this MAP was derived from (step 1-2), each with the consumer where
wrongness lands:

1. **The PLAN's five mechanisms** → the build desk. Cost: the wrong thing gets
   built, correctly.
2. **The two audits' measurements** → the design itself, and every future
   session reading them. Cost: the design rests on a number that is wrong.
3. **The booked items** (lc-156, 158-162) → the build desk. Cost: wrong work,
   or work whose premise died.
4. **The record format file** (dotfiles `claude/investigation-record-format.md`)
   → every session writing a record, and the shipped checker. Cost: the
   checker grades against a definition that does not say what it should.
5. **The shipped code** (`lifecycle verify`, `lifecycle record check`) → any
   repo that runs them. Cost: a false clean or a false finding, silently.
6. **Outward messages** (to the build desk, to peer sessions, to the operator)
   → parties who act on them. Cost: another party acts on a wrong fact.

## Axes

| axis (what against what) | status | last visited (date · round) | yield | next step |
|---|---|---|---|---|
| A1 the PLAN's mechanism claims vs what was actually measured — does each mechanism rest on a measurement, or on the desk's reasoning? | dark (modelled) | — | — | walk each of the five, name its cited basis, check the basis says what the mechanism claims |
| A2 the audits' numbers vs the artifacts they were computed from — recompute, do not re-read | dark (modelled) | — | — | recompute the 55% amend rate and the 44%/39%/19%/17%/5% split independently |
| A3 booked items vs the world they were booked against — the design's own mechanism 5, applied to the design's own bookings | dark (modelled) | — | — | re-ground lc-158/160/161/162 evidence slots at HEAD |
| A4 the format file vs the shipped checker — definition against the thing grading by it | dark (modelled) | — | — | diff the format's stated rules against records.py's predicates |
| A5 shipped code vs its own three-answer contract — do `verify` and `record check` carry could-not-verify where they must? EXTENDED 2026-09-19 to the arc's BUILT mechanism set, and to COUNTS as well as exit codes | dark (probed 2026-09-19 · r3 — effect probes run against the shipped build, pointer: `docs/begehung-findings-2026-09-19-r3.tsv`; no guard holds a mechanism's state VOCABULARY to its documented states, which is where r3's yield sits) | 2026-09-19 · r3 | 5 | 2026-09-19: the exit-code half held everywhere it was probed — the yield moved to the STATE VOCABULARY and the COUNT. Next: sweep the remaining built emitters for the same shape (a state the vocabulary cannot express rendering as its benign neighbour), starting with `lifecycle record check` and `audit`, which this round did not reach |
| A6 outward messages vs what the artifacts actually said — claims relayed to peer/operator, re-read at source | dark (modelled) | — | — | sample this session's peer messages, check each factual claim at its artifact |
| A7 the design's coverage vs the ORIGINAL question — "well scoped and complete before building"; completeness is measured nowhere | dark (modelled) | — | — | name what "complete" would mean and whether any mechanism reaches it |
| A8 label-vs-body drift over the design's own carriers — headings, counts and summaries against their bodies | dark (modelled) | — | — | the design note is 550+ lines and amended 6x today; check its section claims against their bodies |
| A9 enumeration marks — every population the design counts, checked for the chosen-mark failure. EXTENDED 2026-09-19 to the populations the BUILT set counts and scans | dark (probed 2026-09-19 · r4 — red-first run against a COPY of the shipped package, pointer: `docs/begehung-findings-2026-09-19-r4.tsv`; the denominators probed held, the SCAN PATTERNS did not) | 2026-09-19 · r4 | 3 | 2026-09-19: the counted populations held where probed (the --test denominator excludes prose-rest correctly; the structure line's `other` bucket is one honestly-named producer kind). The yield sat in the SCAN's own patterns. Next: the remaining marks in roster.py (`_RESULT_ADD`, `_PROBLEM`) asked the same question the relay pattern failed — does each have a third-verdict sibling — and the ten ident-less could-not-verify sites, which no enumeration currently reaches |
| A10 THE EXISTING CARRIERS vs the design's five mechanisms — items, runbooks, directives, ledger and records were EACH built to solve a piece of this; what do they already cover, where do they overlap, and where does the handoff between them fail so the right action does not fire at the right time (operator-named, 2026-09-18) | prose-covered (the declaration's writer/reader fields carry it; no guard holds the design to them) | 2026-09-18 · r2 | 3 | extend the verb-writer pattern to a session-written kind and measure whether it stops drifting |
| A11 THE NARROWING'S UNIVERSALITY — mechanism 2 assumes CONVERGENT work (hypotheses eliminated toward an answer); is it the right shape for DIVERGENT work where the space is widening, and for routine execution where there are no hypotheses at all? (operator-named, 2026-09-18) | dark (modelled) | — | — | classify this session's own phases as convergent or divergent; check whether a narrowing would have helped or distorted each |
| CROSS-CUTTING lifecycle — per artifact the design holds: where does it live, who writes it, who reads it | dark (modelled) | — | — | the MAP and findings file themselves: is a `.tsv` in docs/ claimed by any registered kind? |
| ENFORCER under its own invariants — the design demands that sessions maintain a narrowing, re-ground evidence at pickup, and write as they work; asked of THIS design and THIS session | prose-covered (the design note's own PLAN section; no guard holds the design to its own mandates) | 2026-09-18 · r1 | 7 | walk A11's boundary into the format's tag vocabulary — a LOCKED tag for decisions |

## Rounds

| round | date | axis | why that axis | read-at | closed-at | reach | class |
|---|---|---|---|---|---|---|---|
| 1 | 2026-09-18 | ENFORCER under its own invariants | operator-named ("we are missing an angle still"); and it is the darkest row by construction — a design that mandates practices has never been held to them | 7a12a88 | 99e998c | 0 hold, 1 superseded | THE DESIGN GENERALISES FROM ONE WORK-KIND. Read across A11, A10 and ENFORCER: mechanism 2 assumes diagnosis-shaped work, mechanism 5's estimate half assumes evidence settles it, and the existing carriers were each built for a different kind. The recurring property is a rule stated universally from a single instance — the corpus's own transfer test, unapplied to the design itself. |
| 2 | 2026-09-18 | A10 THE EXISTING CARRIERS vs the design's five mechanisms | operator-named — items, runbooks, directives, ledger and records were each built to solve a piece of this puzzle, and the design has been ADDING mechanisms without mapping what already covers what | cf14946 | cf14946 | 3 hold, 0 superseded | THE DESIGN RE-INVENTS WHAT THE REPO ALREADY DOES. Read across A10 and ENFORCER: mechanism 2 is the verb-writer pattern already proven on 4 of 22 kinds; the round-1 class (generalising from one work-kind) has a sibling here — generalising from one's own reasoning without first reading what the system already carries, which is the corpus's own read-the-existing-instances rule unapplied to the design. |
| 3 | 2026-09-19 | A5 shipped code vs its own three-answer contract — EXTENDED to the arc's BUILT mechanism set (lc-224, lc-174, lc-176, lc-193, lc-175, lc-179, plus the lc-218 schema chain) | DARKEST by construction, and the extension's natural landing. A5 has never been visited and is the ONLY axis row whose subject is already SHIPPED CODE, which is what this round's commission extends the walk to — the MAP's declared system is the DESIGN, and the extension is noted here rather than by rewriting the header. The design of record makes this axis its own highest-value spend ("the strongest argument yet for spending on the three-answer contract and the denominator rule", docs/answerable-not-felt.md:514). The MAP's interval rule (a row unvisited for the two rounds before this one is itself a finding) fires on A1-A9, A11 and CROSS-CUTTING at this invocation; A5 is the member walked, the rest recorded as a map-hygiene finding. LENS AS REGISTERED: does each built mechanism keep CLEAN / FINDING / COULD NOT VERIFY apart at its OWN emission point — and where a mechanism ships a COUNT instead of an exit code, does the count carry the third state? | 7c62aa6 | 7c62aa6 | 7 hold, 0 superseded — read-at equals closed-at (no code or test change intervened; this round's only writes are its own two docs paths), and every finding was re-tested at its cited line rather than assumed: the read_moments state matrix re-run, the four evaluate_trigger call sites re-grepped, the blocker-exercise count re-read at 0 | THE STATE A VOCABULARY CANNOT EXPRESS RENDERS AS ITS NEAREST NEIGHBOUR, AND THE NEIGHBOUR IS ALWAYS THE BENIGN ONE. Read across A5, A9 and A8: this wave NAMED that class twice while building — lc-174's amended requirement, after the writer buckets turned out unable to express BOTH, and lc-224's, after arity killed the per-kind scalar — and then shipped three more instances of it: a malformed reader `when` rendering as the benign UNDECLARED, a census rendering "predates the mechanism" as "missing", and lc-237's three-call-site enumeration rendering a fourth caller as absent. Rounds 1 and 2 found the same shape one layer up, in the DESIGN (a rule stated universally from one work-kind; a design re-inventing what the repo already does). This is its BUILD-layer sibling — an enumeration complete for the states its author had occasion to see, closing silently over the one they did not. |
| 4 | 2026-09-19 | A9 enumeration marks — every population the BUILT set counts, checked for the chosen-mark failure | REASON NAMED, not momentum: round 3's class predicts further instances specifically at ENUMERATION sites, and A9 is the axis whose subject is exactly that — so this round TESTS round 3's class on a fresh axis rather than re-walking A5 (the transfer test: a rule earns reuse by showing the new case sitting inside its mechanism). SERIES TREND read from the record: r1 ENFORCER yielded 7, r2 A10 yielded 3, r3 A5 yielded 5 — not falling, and each round's findings sat at a different layer (design, carriers, build), so the series is not exhausting a single lens. A9 is also dark and unvisited across both prior rounds, so the MAP's interval rule already names it. LENS AS REGISTERED: for each population the built mechanisms COUNT or SCAN, is the mark keyed to the members already in hand — and would a member carrying the property in another form be returned, or silently absent? | baa5ad6 | baa5ad6 | 4 hold, 0 superseded — the only commit between read-at and closed-at is round 3's own docs-only commit, so no code or test change intervened; each finding was nonetheless re-tested at its cited line (`_RELAY` re-read still keyed to the finding word at roster.py:80, the ten ident-less could-not-verify sites re-counted, ROWS 116 / PROSE_REST 6 re-executed). Two cited line anchors were wrong in the first write of the findings file and were corrected IN PLACE before the commit rather than appended to | ROUND 3'S CLASS SURVIVES THE TRANSFER TEST AND THE TRANSFER SHARPENS IT. Read across A9, A5 and ENFORCER: r3 named the class at the STATE axis and predicted instances at ENUMERATION sites; r4 registered A9 to test that prediction and found one red-first on the MARK axis — a scan widened for the verdict WORDS and not for its own sibling PATTERNS. The addition, not a restatement: THE REPAIR IS WHERE THE CLASS SURVIVES. A widening fixes the member that was found and closes its enumeration over the members already in hand, so the sibling three lines down inherits the original blindness under a comment now asserting coverage — the defect-class-find rule (the artifact carrying the found instance is the first member population) unapplied at the one moment it was cheapest. |
