# Drain wave 1 — lifecycle carrier (desk dotfiles-f1, 2026-09-15)

Four parallel lanes, eight items. Authorized by the judgment desk
(dotfiles-89) on this desk's own `item waves --grouped` read; the
operator's first-hand delegation of the drain desk is on this
session's record (2026-09-15).

Each lane reads: this file's COMMON section, then ITS OWN lane
section, then the TAIL at the bottom. Nothing else in this file
binds you.

---

## COMMON — binds every lane

**Working copy:** `/home/g/dev/Gunther-Schulz/lifecycle` (absolute
paths everywhere; this session touches more than one repo).

**Base commit: `b387ef0`, or any later HEAD whose extra commits
leave YOUR write set untouched.** Read at compose time
(`git -C /home/g/dev/Gunther-Schulz/lifecycle rev-parse --short
HEAD` → `b387ef0`, committed 5 minutes before this file was
written; `git worktree list` showed one checkout, no co-writer
worktrees; `git status --porcelain` empty).

The tolerance is not slack, it is arithmetic: this brief is itself
committed into the copy you work in, so its own commit sits above
`b387ef0` by construction, and your three sibling lanes commit
beside you throughout the wave. Your dispatch prompt names the HEAD
current when you were dispatched. What you check is therefore the
PATHS, not the count.

**Your first act is the base check — three reads, not one:**

```bash
git -C /home/g/dev/Gunther-Schulz/lifecycle merge-base --is-ancestor b387ef0 HEAD
git -C /home/g/dev/Gunther-Schulz/lifecycle log --oneline b387ef0..HEAD
git -C /home/g/dev/Gunther-Schulz/lifecycle status --porcelain
```

Expected: the ancestor check succeeds; the log lists this brief's
own commit and any sibling-lane commits, whose paths are disjoint
from yours — confirm that with
`git diff --quiet b387ef0 HEAD -- <your paths>` and proceed; status
shows no modification to YOUR write set.

A modified file in your write set is a HALT. So is a foreign commit
that touches your write set, OR that touches something your write
set DEPENDS ON — and an import list is not the dependency list: a
file reading a fixture, a carrier or a sibling artifact at runtime
has dependencies no import shows. Report the changed-file list and
halt; this desk confirms the dependency half in one look. Never
rebase, never guess.

**This is a SHARED working copy with three sibling lanes writing at
the same time.** Their file sets are disjoint from yours by
construction (the write-set join). Consequences that bind you:

- Commit by pathspec ONLY: `git commit -m "…" -- <your paths>`,
  every flag before the `--`. Never `git add` then commit, never
  `-A`. The index is shared; a sibling staging between your
  `git status` and your commit rides out under your message
  otherwise.
- A NEW file needs `git add -N <path>` first (naming a FILE, never
  a directory) — intent-to-add, zero content staged, full body
  still committed.
- Never amend. Always a new commit.
- A red in a file OUTSIDE your write set is a sibling lane's
  in-flight edit, not a finding. Re-check it in your private clone
  (below) before reporting it as anything.

**Scratch: your OWN scratchpad**, never this desk's, never the repo.
Name every scratch file with your lane slug.

**Grounding basis — read before building; your report cites what you
actually read:**

1. `dispatch-guards:executor` — the executor skill. **Load FIRST.**
   Conduct, under-report principle, report discipline live there.
2. `/home/g/dev/Gunther-Schulz/lifecycle/CLAUDE.md` — this repo's
   LAWS and its `## Verify` section. The laws bind; read them as
   standalone text, not through this brief's restatements.
3. Your item's own body in
   `/home/g/dev/Gunther-Schulz/lifecycle/ITEMS.md` — requirement,
   done-criterion (the **amended** one where an
   `amended-done-criterion` slot exists — it supersedes), evidence,
   must-not-moves. The item body is normative over this brief's
   summary of it; a divergence between the two is a gap to report.
4. The **dotfiles** repo's `CLAUDE.md`, section
   `## Registered procedure: guard/checker builds (§6 class devbook)`
   — the class devbook, and it binds every item in this wave. Your
   dispatch prompt names that repo's absolute path on this machine;
   this file is public history and does not carry it.
   **Section sha256 = `dbba28a0a99daf8c44833ef7644c79561e7b8ae7c7be9c6501103abe8e1bb081`**
   (computed by this desk from the FILE at compose time: from the
   `## Registered procedure` heading line to exclusive of the next
   `## ` line, splitlines keepends, joined, utf-8 — matches the
   register's stored fingerprint in that same repo's
   `claude/readiness.json`).
   Your session may have been INJECTED an older snapshot of that
   text. If what you hold does not hash to the pin, **read the
   file** — the file wins, and say so in your report.

**The verify battery** (this repo's own `## Verify` section):

```bash
python3 -m unittest discover -s test -p 'test_*.py'
python3 plugin/cli/lifecycle --test
python3 tools/prove-rows.py
python3 plugin/cli/lifecycle audit
```

**`tools/prove-rows.py` MUTATES core files in the tree it runs in,
and restores them at the end of each arm. NEVER run it in the shared
working copy.** This desk did exactly that on 2026-09-15, killed it
mid-arm, and left a live one-line mutation standing in
`plugin/cli/lifecycle_core/migrate.py` — that incident is lc-133's
own evidence, and with three sibling lanes writing beside you the
blast radius is theirs, not just yours. Run it in a **private clone**:

```bash
git clone /home/g/dev/Gunther-Schulz/lifecycle <your-scratch>/<slug>-clone
git -C <your-scratch>/<slug>-clone checkout <the commit you want to grade>
```

The whole battery runs **in that clone**, never narrowed — no `-k`
selection, no `-x`. Answer with the failure LIST, not a count.

**The clone's own self-check must be GREEN FIRST, before any red
from it is trusted.** A red produced by a broken arrangement is
indistinguishable from one produced by the defect, and the
arrangement is the basis. Read unittest's `failures=` vs `errors=`
split PER ARM: an ERROR proves only that the code is new (a fixture
reaching through a name the old side lacks); only an assertion
FAILURE at the defect is a red that counts.

**Red-first, for every item:** the OLD side is a private clone at
`b387ef0` (before your change). Demonstrate the gap with a real run,
output verbatim in your report. Then build. A check that pins an
already-true property proves itself by TWO mutations — of the build
it grades, and of its own arrangement — both assertion failures.

**Restoring a deliberately modified working-tree file: restore from
a copy you took BEFORE the modification. Never `git checkout --`,
never `git restore`, never `git stash`.** This binds any
mutate-and-restore you run, for any purpose, not only a formal bite.
Those commands are whole-file destructive against uncommitted work
and have destroyed real work in this repo family three times.
Confirm a restore by sha256 of the working file against
`git show HEAD:<path>`, never by `git status` alone.

**Commit plan / guards — the read that found them:**
`git config --get core.hooksPath` →
`~/dev/Gunther-Schulz/dotfiles/git/hooks` (global, set in both the
repo and global config). That global `pre-commit` CHAINS the
repo-local `plugin/hooks/pre-commit`, which checks only STAGED
CARRIERS (`ITEMS.md`, `ITEMS-DONE.md`, `LEDGER.md`,
`.claude/lifecycle.json`) against the shape the tool writes. **No
lane in this wave stages a carrier**, so that gate does not fire for
you. The global pre-commit runs no repo-wide test suite (grepped:
its `unittest`/`--test` hits are its own self-check), so a sibling's
transient red cannot block your commit. No version bump, no manifest
bump, no ordering constraint against a guard.

**One commit per ITEM**, not per lane — per-change attribution.

**Your commit trailer is named in YOUR OWN lane section below, and
it differs by lane because this wave runs two tiers.** Where the
TAIL at the bottom of this file names a different model, your lane
section wins — the TAIL is the standard pasted block and its
`<model>` is a fill-in, filled per lane here.

**Commits stay UNPUSHED.** Pushing is this desk's act after
verification.

**Do NOT touch the carriers.** You do not grade, close, amend or
promote any item. This desk books every closure. If your work
finishes an item, say so in your report and stop there.

**Critique pass — before your first build call**, send ONE message:
which Background line in this brief you find unopened or wrong, and
which two lines of it contradict each other. Then continue without
waiting for a reply.

**On a gap: HALT THE ITEM, finish the remainder of your lane,
REPORT.** Never halt the lane — a lane that stops and waits cannot
be reached; messages reach you only at a turn boundary.

---

## LANE A — `sonnet-lc132-135`

Commit trailer for this lane:
`Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`

Write set (yours alone):
`plugin/cli/lifecycle_core/items.py`,
`test/test_item_check_staged.py`,
`test/test_repair.py`

Both items write `items.py`, so they are SEQUENTIAL in this lane,
each with its own commit and its own red-first arrangement. Order:
**lc-132 first, then lc-135.**

### lc-132 — the could-not-verify message moves to `out()`

`item check --staged` writes its COULD NOT VERIFY message to
STDERR, against this repo's own idiom. Desk measurement 2026-09-15
over `plugin/cli/lifecycle_core/*.py`: COULD NOT VERIFY reaches the
user via `out()` **104** times and via stderr **twice** (the two
being `refusals.py:211` and `retire.py:355`). Existing instances fix
the idiom — this repo's own law, and the WHAT-SHAPE axis of fix
placement.

Settled design, implement exactly this:

- `item check --staged` renders COULD NOT VERIFY through `out()`
  like every other verb in this repo.
- **Exit code unchanged at 3.**
- **One home for the message — never both streams.**
- **MUST-NOT-MOVE: the message TEXT is unchanged.** This is a stream
  change, not a prose change.
- The existing `AStagedDeletionIsNotClean` arm and the
  newly-introduced-could-not-verify arm both KEEP their subjects;
  only the stream moves.

Red-first: assert the message on STDOUT against the current build,
which writes it to stderr — an assertion **FAILURE**, not an error.

### lc-135 — reconcile the repair verb's judgment classes against the checker's residue

`item repair --shape` reports its judgment classes in a SECOND
VOCABULARY over findings the registry already carries, and nothing
checks that the two agree. Measured at the desk on copies of
dotfiles' carriers: the verb's JUDGMENT classes map one-to-one onto
registered rows — closed-still-blocked → `blocked_in_done_home`
(4/4), missing-slot and unknown-slot → `item_shape` (5/5) — and the
verb's listed set equalled `item check`'s residual set EXACTLY over
bodies, zero unmatched either way. The two axes are each defensible
(a row says WHAT IS WRONG, a judgment class says WHAT THE VERB
DECLINED TO REPAIR AND WHY); what is missing is anything holding
them in correspondence.

Settled design: a check that reconciles `item repair --shape`'s
listed set against `item check`'s residual findings over the SAME
carrier, and FAILS when they diverge — **over bodies, never over
counts**. Equal counts are not agreement. Today's reconciliation
exists only because a desk ran it by hand.

Read the item's full `done-criterion` in `ITEMS.md` before building;
it is longer than this summary and it governs.

---

## LANE B — `opus-lc133-51-30`

**This lane runs on OPUS, not sonnet, and the other three do not.**
Ruling by the judgment desk (dotfiles-89, 2026-09-15), on a
mechanism checkable here: the standing default sentence is
"brief-covered execution and discovery DEFAULT TO SONNET; the
verdict-stage reviewer default and REGISTER CERTIFICATIONS
override", and `guard-checker-bau` is registered at tier **opus**.
Its `eval-open` status governs certification STATUS — the class is
re-earning after the df-250 devbook edit, so runs under it are
graded — and never tier APPLICABILITY. Reading `eval-open` as
lapsed would mean every fingerprint edit silently demotes a class
to the default, which is a downgrade nobody decided. This lane
rewrites the proof instrument and repairs refusal machinery:
squarely the registered class.

Because the class is re-earning, **your run is graded evidence**.
Report as if the class certification depended on it, because it
does.

Commit trailer for this lane:
`Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`

Write set (yours alone):
`tools/prove-rows.py`,
`plugin/cli/lifecycle_core/roster.py`,
`test/test_refusals.py`

Three items, **strictly in this order**, each with its own commit:
**lc-133 → lc-51 → lc-30.** The order is this desk's call and the
reason is load-bearing: lc-133 makes the proof instrument safe to
run before lc-51 and lc-30 mutate it further.

**You are editing the instrument every other proof in this repo
depends on.** After each of your commits, the clone you grade in is
cloned FROM the live repo and checked out at YOUR commit — so
lc-51's and lc-30's arms run against an instrument that already
carries lc-133's refusal. Expect that; it is the point.

### lc-133 — prove-rows restore is not crash-safe

`tools/prove-rows.py` mutates a core file, runs the roster, and
restores by file copy at the END of each arm — so a run interrupted
mid-arm (SIGKILL, harness timeout, operator Ctrl-C) leaves the
injected mutation LIVE in the tree with no marker that it did.
Demonstrated at this desk 2026-09-15 on the live shared checkout:
killed mid-arm, a one-line mutation left standing in
`plugin/cli/lifecycle_core/migrate.py` (`duplicate_bodies`, the
`known.get` lookup replaced by `None`), found only by sha256-ing all
64 tracked files against their HEAD blobs.

Settled design:

- prove-rows **REFUSES to start** when any file it would mutate
  already differs from HEAD, naming the file and the difference.
- The refusal compares **sha, never grep**.
- Restores run under **try/finally**, so an ordinary exception
  cannot leak a mutation.

Red-first BOTH ways, both assertion FAILURES:
1. plant a divergence in a mutation target, show the refusal fires
   under the finding code;
2. raise inside an arm, show the file equals its HEAD blob
   afterwards — which the current build fails.

### lc-51 — anchors match line-exact; two rows gain arrangements

prove-rows anchors its mutation arrangements by SUBSTRING, so an
unrelated verb spelling the same line at a deeper indent silently
retires another row's proof. `move_uncommitted`'s anchor is
`    if r.returncode != 0:` at 4-space indent — a substring of the
same line at any deeper indent. Separately, the two rows added for
lc-44/lc-49 (`blocker_unstorable`, `closed_ref_unresolvable`) have
no mutation arrangement at all.

Settled design:

- Anchors match **LINE-EXACT**, not by substring.
- `blocker_unstorable` and `closed_ref_unresolvable` each gain a
  recorded mutation arrangement.

Red-first (already in hand, reproduce it): a copy of
`move_uncommitted`'s anchor line indented one level deeper elsewhere
in the file makes prove-rows report that row's source as moved —
measured live: exit 0 → EXIT 3, with `move_uncommitted` and
`blocked_in_done_home` both reporting "the source moved under this
arrangement".

MUST-NOT-MOVE: prove-rows stays **exit 0** over the existing
arrangements, and the honest **COULD NOT VERIFY** answer is
preserved — that answer is what made this catchable.

### lc-30 — the route-set check's asymmetry

The ROUTE SETS check is asymmetric: a route the refusal TEXT names
but nothing watches is a FINDING (`route_set_unwatched`), while the
reverse — the code routing a shape through a refusal whose text does
NOT name it — prints a note and sets no code. `roster.py:177`
computes `stray = watched - full`; `:183-187` prints it as a note
with no `exits.worst` call, while `:188-195` makes the mirror case
(`missing`) a FINDING. So a refusal can catch more than it says, and
the operator reading the finding gets a WRONG cause for their entry.

Settled design: a refusal whose text is narrower than what the code
routes through it **FAILS** the ROUTE SETS check rather than noting
it.

Red-first on exactly the lane-A state: two ambiguous-closure shapes
routed through `migration_unclassified`, whose text names only the
no-rule case. Green once the text covers what it catches, or the
shapes get their own row.

Note for your grounding: the item's `blocked-by` slot carries an old
evidence blocker, and its `amended-blocked-by` (2026-09-13) reads
`NONE`. The amendment supersedes — the item is unblocked.

---

## LANE C — `sonnet-lc119`

Commit trailer for this lane:
`Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`

Write set (yours alone):
`plugin/cli/lifecycle_core/init.py`,
`test/test_init.py`

### lc-119 — an unresolved declaration value reaches the exit code

`init` prints `COULD NOT VERIFY` for a value it could not resolve
and still **exits 0**, so a machine reading the exit code cannot see
that a declared value was guessed. Verified at the drain desk
2026-09-13 at `288bfba`: init exercised in a scratch repo with no
git remote exits 0 while printing
`public: False — COULD NOT VERIFY: this repo has no git remote…`.

**The decision this item waited on is ANSWERED** — operator
decision 2026-09-13, `LEDGER.md:79`, all-as-recommended via the
judgment desk: **THE EXIT CODE CARRIES IT.** Do not re-open it, and
do not settle any part of it at your own tier.

Settled design (this is the item's `amended-done-criterion`; read it
in full in `ITEMS.md`, it governs):

- `init` exits `COULD_NOT_VERIFY`, not `CLEAN`, whenever any
  declared value was unresolved.
- **Every existing CLEAN assertion in `test/test_init.py` is
  re-examined ONE BY ONE** as either legitimately-CLEAN or
  legitimately-changed. **NEVER BULK-UPDATED** — bulk-updating
  assertions to match a new behaviour is how a suite stops testing,
  and it is the one move that would make this item look done while
  removing the coverage it rests on.
- **THE POPULATION IS DERIVED, NOT COUNTED FROM THE ENTRY.**
  Enumerate the assertions from the FILE at build time. The entry's
  own history is the warning: it said "the fourteen existing CLEAN
  assertions", and a later grep returned 21. Any number in this
  brief or in that entry is stale by construction — derive it, and
  report the number you derived.
- **MUST-NOT-MOVE:** every existing could-not-verify LINE keeps its
  exact text (this is an exit-code change, not a prose change); and
  the guards lc-118 landed on all four subprocess calls stay intact
  — an unrunnable git already yields could-not-verify by that route,
  and the two paths must not double-report.

Red-first: a fixture whose declaration carries an unresolved value
exits CLEAN today; assert `COULD_NOT_VERIFY` — an assertion
FAILURE, not an error.

---

## LANE D — `sonnet-lc31-63`

Commit trailer for this lane:
`Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`

Write set (yours alone):
`plugin/cli/lifecycle_core/cli.py`,
`test/test_migrate.py`,
`test/test_verbs.py`

Two independent items, no shared file between them. Do them in
either order, **one commit each**.

### lc-31 — a repeated `--from` silently discards the first source

Executed by lane B (2026-08-27):
`build_parser().parse_args(["migrate","--from","A.md","--from","B.md"])`
returns `from_carrier="B.md"` — the first source is discarded with
no output. argparse overwriting is a silent wrong answer at the
migration entry point: the caller believes two sources were read and
one was.

Settled design: a second `--from` in ONE invocation **REFUSES**,
with this message text:

```
one --from per invocation; use --merge for a second source
```

Red-first on the executed two-`--from` parse. The single-`--from`
invocation is unchanged.

Distinct from lc-17's merge mode, which is a second INVOCATION; this
is one invocation naming two sources. Do not touch merge mode.

### lc-63 — `item promote` gets its own red for the block-boundary defect

`item promote` has no red of its own for the lc-40 block-boundary
defect. It is the second caller of `_set_slots`
(`verbs.py:1692`; park's call is `verbs.py:1558`) and inherits the
fix through the shared helper — which is inference, not an executed
arm. A red certifies the CLASS that fired, never the instrument's
reach: park and promote are two variants because they write
different slots.

Settled design: mirror the existing park arm. Read
`test/test_verbs.py` around lines **701-802** first — that block is
the lc-40 class, it carries the tab plant (`tab = "##\txx-2"`) and
the park arm's own arrangement, and it is the template you follow,
including how it obtains the OLD boundary code.

The new arm:

- a carrier whose SECOND block heading is tab-separated,
- a `promote` naming the **FIRST** block,
- an assertion that the second block was **NOT** re-graded,
- **plus the must-move companion** showing the named block IS still
  written — the pair is what separates the defect from a build that
  stopped writing.

This item writes `test/test_verbs.py` ONLY. If you conclude the
production code needs a change, that is a gap to REPORT, not to
build — it would be a finding about `_set_slots` that the item does
not commission.

---

## TAIL — binds every lane

A mid-run message may not arrive before the turn ends: on a gap
HALT THE ITEM, FINISH THE REMAINDER, REPORT — never halt the
LANE, since "halt and wait" is not a survivable state for a
subagent (source: §2, the delivery binding).
Closing report (mandatory; the project's own report form if it
defines one, else the §2 form here — never both; "none" is a
valid slot answer, silence is not): (a) items completed w/
evidence, (b) checks RUN w/ real output — FULL counts incl.
skips (`N passed, M failed, K skipped`), each skip dispositioned
(which check, why, whether the reason touches the item); a skip
in a check YOU built is a finding, not a pass — the built branch
did not execute, (c) gaps surfaced —
incl. anything needing a tier above yours, returned as a question
with its evidence, never settled at your tier,
(d) deviations w/ reason, (e) candidate lessons, (f) files
touched + commit hashes (unpushed) — only commits whose
Co-Authored-By trailer is YOURS; one you cannot claim by
trailer is "present in the tree, not mine"; a `.git/config`
write counts as a repo write, (g) what was NOT verified,
(h) sources actually read, of those the brief named.
Every claim about something OUTSIDE your own work — a file you
did not write, a mechanism, another repo, a tool's behavior —
names the read that opened it, or carries "inferred,
unverified"; a recommendation resting on an unopened claim
carries the grade too.
Drain your inbox before sending, and between parts of a
multi-part report: every dispatcher message received up to send
time is dispositioned or named as unhandled.
Message ≤3000 chars each: a report longer than one message is
SPLIT into labeled parts (1/N) — do NOT write a report FILE
(harness-blocked for subagents); supporting data goes to the
brief's assigned DATA files, the message carries key findings
+ any such paths. A missing decision, file,
or value is surfaced as a gap, never bridged with a guess.
A check that got backgrounded is AWAITED before the closing
report (TaskOutput block=true on its task id) — ending your
turn orphans it; a report sent with a check still running is
an INTERIM report, says so, and names what remains.
Commits unpushed, by pathspec — `git commit -m "…" -- <paths>`
with every flag BEFORE the `--` (after it git reads `-m` as a
pathspec and the commit fails; `-F` for a multi-line message),
never
`git add` then `git commit` and never `-A`: the index is shared,
so a co-writer staging between your `git status` and your commit
rides out under your message whatever you added. A NEW file is
invisible to a pathspec commit until `git add -N <path>`
registers it (intent-to-add: zero content staged, full body
still committed). Trailer: the one named in YOUR OWN lane
section above — this wave runs two tiers and the lane section is
where `<model>` is filled.
Never amend — always a new commit: the amend-gate denies
subagent amends regardless of ownership (source: §1 amend
rule).
After sending the report your write grant is over: a defect you
find later is REPORTED, never edited or amended (source: §4
ownership rule).
