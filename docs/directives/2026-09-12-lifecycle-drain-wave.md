# Lifecycle drain wave (kickoff 2026-09-12)

Judgment desk: dotfiles-85 [296b0d] (fable). Drain desk: the
operator-opened session that acknowledges this directive
(dotfiles-b8). Delegation is inert until the operator states it
first-hand in that session; the wave starts only after the drain
desk acknowledges to dotfiles-85 that the operator's confirmation
line is on its record.

## Mission

Drain this repo's READY set. The carrier's own ratio finding fires
first and is binding: `item ratio` reports 3.20:1 against the 3:1
tripwire (measured 2026-09-12), so a retirement pass runs BEFORE
any new booking — stale-risk entries re-checked against the world,
the overtaken dropped with one-line reasons, duplicates merged.
Most READY grades are as-of 2026-08-26/27 and the tool has moved
since.

Head of the drain after the retirement pass (operator ordering —
consumers are waiting on these two):

- **lc-73** — duplicate-disposition design. Consumer: statiker's
  st-22, and through it st-39 (that repo's carrier deletion, run
  by its own sessions once lc-73 lands).
- **lc-86** — migrate deletes-with-record. Consumer: CachyOS-Setup's
  booked carrier retirement and every future migration. Family
  spelling for citation pins: `at blob <sha>` (operator ruling
  2026-09-12).

## Boundaries

- This desk owns the lifecycle working copy and its carrier verbs.
  One writer per working copy; claim log and push as separate
  invocations.
- An item whose realizing write lands OUTSIDE this repo (several
  entries name cache-fix paths in their write-sets) is surfaced to
  dotfiles-85 with its write boundary named, never executed from
  here.
- Corpus writes (claude modules, JOURNAL, records under the
  dotfiles repo) stay at the judgment desk.
- Operator decisions travel through dotfiles-85 as numbered rounds.
- The dotfiles working copy belongs to the dotfiles drainage desk
  (dotfiles-ef); nothing here touches it.

## Conduct

Digests to dotfiles-85; blockers, decisions, and milestones
immediately. dotfiles-85 arms the wait horizons — this desk's
announced cadence is never the waiting side's instrument.

## PAUSE STATE — drain desk dotfiles-b8, 2026-09-13 (operator pause)

Written here rather than left in chat: the arc outlives the session.
Everything below is pushed; `origin/main` at `fcedb60`, tree clean.

**DONE — the retirement pass.** Binding because the carrier's own
ratio finding fired: 80:25 = 3.20:1 at intake, **83:37 = 2.24:1 CLEAN**
now, the tool's verdict. Twelve items left the carrier: ten closed
DONE against named landing commits, two DROPPED. Reconciliation
53 kept + 10 + 2 = 63 at intake; conservation CLEAN throughout.
44 stale-risk entries verdicted by four read-only sonnet lanes, every
OVERTAKEN verdict re-run at this desk before it was acted on.

THE INTAKE DIAGNOSIS WAS WRONG AND THE CORRECTION IS THE FINDING:
the banner said capture-dominance, but nine of the twelve departures
were work FINISHED AND NEVER CLOSED, with landing commits naming the
items (`9f8350f`, `f3f7517`, `120c733`, `cbfaee6`, `a146b62`). A
blocked-exit component inside a flow alarm — the drain figure
undercounted, so the ratio read worse than real throughput. Cure was
closure with refs, never drops. `lc-20` took the other exit
deliberately: its criterion was satisfied in code at `96bb599` (20:19)
BEFORE its own booking commit `40b9c36` (21:31) the same evening, so
it was never a live defect and DONE would have credited work nobody
did.

Also corrected: the 2026-09-12 re-confirmations on `lc-13` and `lc-14`
were FALSE — each checked only the lane verb surface while the fix had
been in the declaration validator since `120c733`, two weeks earlier.
A scoped read settling an unscoped absence. Recorded in both closure
reasons as a method finding.

**MID-FLIGHT — `lc-73`, and exactly where.** Build LANDED and
unit-proven at `a9f3a26` (opus lane, class `guard-checker-bau`,
devbook pin `a6a90725…` verified against the file). Proven: the
provenance detector over raw blocks of both homes, the automatic
counted skip, the refusal preserved and narrowed, a four-term
reconciliation identity. Red-first at CLI altitude against a copy of
statiker pinned to `865e0a2` (each copy sha256-equal to that commit's
blob): OLD binary 17, NEW binary 20, with `st-8`/`st-10`/`st-14` — the
decayed three — present only in the new. Suites 337 OK / 0 skipped,
roster 73/73 CLEAN.

NOT DONE, and it is the entry's other half: the live merge into
statiker is **statiker-58's act**, run deliberately as `st-22` or
against a copy first. It has NOT run. `lc-73` stays OPEN for that
reason.

**GATE ON THAT RUN — `lc-92`.** The lc-73 lane stopped on it rather
than repairing it, correctly: `per_source_counts` still reads the
amendment-resolved slot over the live carrier only — both readings
lc-73 replaced in the detector, one function over. So `st-22` will
exit 3 with TOTAL conservation clean and every body on disk; what
cannot be verified is which run put them where. `lc-92` must land
BEFORE `st-22`. Its red case already exists and goes red when the
repair lands.

**QUEUED.** `lc-86` (migrate ends with the source DELETED, not frozen;
citations carry the pinned blob inline — family spelling
`at blob <sha>`) is the head. Then the rest of READY. Booked this
session and not yet built: `lc-89` (pre-push prints a fatal line and
an empty ref name; coverage is correct, the noise trains the discount
reflex), `lc-90` (item close mints `blocked_in_done_home` for an
id-blocker whose target is already closed — mechanism relayed from
dotfiles-ef and UNVERIFIED here, re-read it first), `lc-91` (lc-19's
unbuilt half, repo-declarable closure vocabulary), `lc-92` above.

**OUTSIDE THIS DESK'S BOUNDARY, unchanged:** `lc-24`, `lc-66`, `lc-67`
name claude-code-cache-fix paths; `lc-73`'s production run is
statiker's. `lc-14` and `lc-43` also named cache-fix paths and are now
closed, so that list shrank by two.

**INSTRUMENT LESSONS FROM THIS SESSION, for whoever runs the next
pass.** Three of this desk's own, all self-caught: `$?` after a
pipeline is the LAST stage's status, so a `prove-rows` baseline of
"exit 0" was written into a dispatch brief when the real answer is 3;
an unquoted `$VAR` does not word-split under zsh, so nine batched
closes failed at once and `>/dev/null` rendered the failures as
silence; and running `prove-rows` in the background against the same
checkout while a suite runs produces a dirty tree and spurious
failures that look like a lane's defect — the `M` was git's stat cache
over a byte-identical file. One for dispatch-guards, whose repo this
desk does not write: the read-only tail's REPORT-CHANNEL line names
the dispatcher's own session name, which a subagent cannot resolve —
two of four lanes hit it independently.

AND THE ONE WORTH THE MOST, named by the lc-73 lane after its close
because it is invisible to the party it hits: **the one-writer rule
governs COMMITS on a shared copy and says nothing about transient
mutate-and-restore probes**, which write real bytes to real tracked
files for real seconds. `prove-rows` does it by design, and so does
every red-first bite the guard/checker devbook prescribes. So two
parties can both be obeying one-writer and still interleave writes to
the same path, with neither instrument reporting it — each just sees
an inexplicable result and re-runs. This desk got the LOUD direction
(a stat-cache `M` on a byte-identical file, plus three suite failures
that were the suite grading a momentarily mutated source). The QUIET
direction is the hazard: a bite arm reading "restored clean" over a
co-writer's live mutation is byte-indistinguishable from a bite that
restored correctly, and `git update-index --refresh` defeats the stat
cache but NOT a concurrent writer. It bit two parties on one file
inside a single lane today. Coverage question for the devbook's step 4
or the dispatch skill's shared-copy rules; travels to the judgment
desk with the CLI-path lesson above.
