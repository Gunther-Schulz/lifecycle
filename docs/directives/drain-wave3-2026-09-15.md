# Drain wave 3 — lifecycle carrier (desk dotfiles-f1, 2026-09-15)

Three lanes, four items. Authorized by the judgment desk on this
desk's own item-level join.

Each lane reads: COMMON, then ITS OWN lane section, then COMMIT PLAN
and TAIL. Nothing else here binds you.

---

## COMMON — binds every lane

**Working copy:** `/home/g/dev/Gunther-Schulz/lifecycle`, absolute
paths everywhere.

**Base: `173ae1e`, or any later HEAD whose extra commits leave YOUR
write set untouched.** Read at compose time (`rev-parse --short
HEAD` → `173ae1e`, `status --porcelain` empty). Two sibling lanes
AND THIS DESK commit into this copy throughout — the desk is a party
inside your boundary, not an exception to it. Check PATHS, never
commit counts:

```bash
git -C /home/g/dev/Gunther-Schulz/lifecycle merge-base --is-ancestor 173ae1e HEAD
git -C /home/g/dev/Gunther-Schulz/lifecycle log --oneline 173ae1e..HEAD
git -C /home/g/dev/Gunther-Schulz/lifecycle status --porcelain
git -C /home/g/dev/Gunther-Schulz/lifecycle diff --quiet 173ae1e HEAD -- <your paths>
```

A modification to your write set is a HALT; so is a foreign commit
touching it or something it DEPENDS ON. Report, never rebase.

**Scratch: your OWN scratchpad**, every file slugged with your lane
name.

**Grounding — read before building; your report cites what you read:**

1. `dispatch-guards:executor` — **load FIRST**.
2. This repo's `CLAUDE.md` — INVARIANTS, LAWS (1, 2, 3, 8, 17, 24
   especially), `## Verify`, and the prove-rows paragraphs.
3. Your item's body in `ITEMS.md`. **Law 8 tells you how to read
   it:** `lifecycle item slots <id>` gives the slots with amendments
   RESOLVED — current truth — and DROPS `amend-reason` lines, where
   the reasoning lives. Read BOTH. A truncating read of the raw
   block drops exactly the lines that supersede.
4. The **dotfiles** repo's `CLAUDE.md`, section
   `## Registered procedure: guard/checker builds (§6 class devbook)`
   — binds every lane here. Section sha256 pin:
   `dbba28a0a99daf8c44833ef7644c79561e7b8ae7c7be9c6501103abe8e1bb081`
   computed from the FILE. Your prompt names the absolute path; this
   file is public history and does not carry it. If your injected
   copy does not hash to the pin, read the file and say so.

**The battery, in a PRIVATE CLONE, never the shared copy** —
`prove-rows` mutates tracked files in whatever tree it runs in:

```bash
python3 -m unittest discover -s test -p 'test_*.py'
python3 plugin/cli/lifecycle --test
python3 tools/prove-rows.py
python3 plugin/cli/lifecycle audit
```

Whole battery, never narrowed — no `-k`, no `-x`. Answer with the
failure LIST and full counts. Read `failures=` vs `errors=` per arm:
an ERROR proves only that the code is new.

**NEVER RUN A MUTATING TOOL IN A CLONE YOU ARE ALSO TESTING IN.**
This desk did exactly that last wave — backgrounded `prove-rows` in
a clone, ran the battery there, and read three failures of its own
making. Separate clones, or run them in sequence.

**BASELINE at `173ae1e`**, measured here: battery run=610
failures=0 errors=0 skipped=1; `--test` CLEAN at 88 rows;
`prove-rows` exit 0 at 75 of 88; `audit` exit 3 with TWO findings
(`kind_grew_without_exit`, `laws_scope_audit`). The one skip is
`test_verbs.LedgerStorableBlocker.test_the_67_REPAIRED_dotfiles_TEXTS_all_pass_and_the_OLD_ONES_do_not`
— ARRANGEMENT-dependent: it grades real texts in a sibling checkout,
so it SKIPS in a clone and RUNS in the real checkout. `skipped=0`
and `skipped=1` are both correct; neither refutes the other.

**EVERY NUMBER HERE IS A CONDITION, NOT A FACT.** Derive it and
report a disagreement rather than matching mine. Derive the roster
count TWO independent ways and require agreement:

```bash
grep -c '^    Row($' plugin/cli/lifecycle_core/refusals.py
python3 plugin/cli/lifecycle --test | grep '^rows:'
```

A `grep -c` alone returns the same zero for "none exist" and "the
pattern never matched". A disagreement between the two is a finding.

**EVERY CITATION HERE IS A CONDITION TOO — new this wave, and it is
the desk's own repair.** Where a line below names a symbol, it names
the COMMAND that finds it, not a line number I remember. Last wave
four of this desk's reversals were locations and boundaries rather
than counts: a function cited in the wrong file because the desk
relayed a lane's report without opening it, a test named wrong
twice. Run the command. If it disagrees with the file I name, that
is a finding and I want it.

**TWO NUMBERS ARE LIVE THIS WAVE and will move under you:**
- `audit`'s finding count goes 2 → 3 when lane 1's second item
  lands. **That is the POINT, not a regression** — a third finding
  that was always true and unreported is the walk becoming honest.
  Do not pin it; derive it at your own commit.
- The roster count moves if any lane registers a refusal.

**Red-first — pick your branch:** (1) new behaviour → OLD side is a
clone at `173ae1e`; (2) proving an existing fix → the commit that
PREDATES the fix, named in the item's evidence; (3) pinning an
already-true property → no red exists, so TWO mutations, of the
BUILD and of the ARRANGEMENT, both assertion FAILURES. Unsure which
branch is a gap to REPORT.

**A file-based mutation PRINTS its confirmation** — the changed line
read back off disk, or a sha256 that moved — checked before the
run's outcome counts either way. A mutation that never applied
returns a green identical to a real one; the desk hit this twice
last wave. In-process mutations fail loudly and need no such line.

**RESTORE from a copy taken BEFORE the change** — never
`git checkout --`, `git restore`, `git stash`; a guard will deny it
and the guard is right. Confirm by sha256 against
`git show HEAD:<path>`, never by `git status`.

**Commit by pathspec**, flags before the `--`, never `git add` then
commit, never `-A`, never amend. `git add -N <path>` for a new file.
ONE commit per ITEM. UNPUSHED — this desk pushes after verifying.

**TRAILERS DO NOT IDENTIFY YOU**: every subagent of this session
inherits the parent's `Claude-Session` URL and opus lanes share a
`Co-Authored-By`. Claim commits by CONTENT — the paths they touch.

**Do NOT touch the carriers.** This desk books every closure.

**LAW 24 BINDS YOU AT BUILD TIME:** if your work emits a NEW
`FINDING [...]`, it needs a registered row in `refusals.py`. If that
file is not in your write set, HALT the item and report — do not
drop the bracket tag to quiet the scanner, and do not reach outside
your boundary.

**Critique pass — before your first build call**, send ONE message:
which Background line you find unopened or wrong, and which two
lines contradict each other. Then continue without waiting.

---

## COMMIT PLAN — guards, each with the read that found it

- `git config --get core.hooksPath` →
  `~/dev/Gunther-Schulz/dotfiles/git/hooks`. That global pre-commit
  CHAINS the repo-local hook.
- `ls -l .git/hooks/pre-commit` → symlink to
  `../../plugin/hooks/pre-commit`, which checks only STAGED CARRIERS
  (`ITEMS.md`, `ITEMS-DONE.md`, `LEDGER.md`,
  `.claude/lifecycle.json`). No lane stages one, so it does not fire
  for you; it still prints a counted line, which is not a finding.
- **A payload-version guard EXISTS and does not apply**, stated with
  its basis because this repo IS a plugin payload: the global
  pre-commit opens on "plugin payload changed without a version
  bump", and `plugin/.claude-plugin/plugin.json` stands at 0.3.25.
  `lifecycle` is absent from `~/.claude/plugins/installed_plugins.json`
  (checked; nine installed keys, none this repo), so there is no
  installed version to compare against — and waves 1 and 2 landed
  fourteen payload commits at an unchanged 0.3.25 with no bounce.
- **NOBODY BUMPS THE MANIFEST.** If a commit of yours DOES bounce on
  that guard, the basis above is refuted: HALT and report rather
  than bumping, which would be an unowned release act.
- No repo-wide suite runs at commit, so a sibling's transient red
  cannot block your commit.

---

## LANE 1 — `opus-lc148-145`

Trailer: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`

Write set: `plugin/cli/lifecycle_core/items.py`,
`plugin/cli/lifecycle_core/retire.py`, `test/test_items.py`,
`test/test_retire.py`

**Two items, STRICTLY in this order, one commit each: lc-148 →
lc-145.** They share `retire.py`, so they serialize here rather than
running as two lanes.

### lc-148 — a compacted id must never be re-issued

Last wave's compaction verb made id reuse reachable. Find the
allocator and read its docstring first:

```bash
grep -rn "def next_ident" plugin/cli/lifecycle_core/items.py
```

Its stated reason for correctness is that EVERY home is read, live
and closed. Compaction is the first verb that removes a body from
every home, so it invalidates that exact sentence. Measured: after
compacting an id, `item add` re-issues it while the ledger already
carries the compaction record, and `item check` reports CLEAN
throughout.

**THE DESIGN IS RULED — do not re-derive it.** The repair RESTORES
the docstring's premise rather than bolting a second mechanism
beside it: the compaction record becomes a home `next_ident` reads.
The ledger's compaction decision line already carries the folded
ids, and `ledger.parse_line` already reads that line back — find it:

```bash
grep -rn "def parse_line" plugin/cli/lifecycle_core/ledger.py
```

Reuse that parser; do not write a second one.

**The alternative is REFUSED on this repo's own invariant 3**, not
on preference: a separate retired-id or tombstone store would be a
SECOND HOME for a fact the ledger already holds. If you think
invariant 3 does not reach it, that is a gap to REPORT, not a design
call to make.

Red-first is the measured walk: in a scratch repo, add an item,
close it, compact it, then `item add` and assert the new id is NOT
the compacted one — an assertion FAILURE today.

MUST-NOT-MOVE: ids stay immutable across ORDINARY moves; the
allocator still refuses with no `id-prefix` declared; and **a repo
whose ledger has no compaction line allocates exactly as today** —
assert that, since a change making every allocation consult a
missing file would pass a reuse test while breaking every fresh
repo.

### lc-145 — the retire walk reports the done-bodies kind honestly

`PERFORMED_EXITS` does not list `compact`, so that kind reads NOT
CHECKED rather than as a kind that grew without an exit. Locate it:

```bash
grep -rn "PERFORMED_EXITS" plugin/cli/lifecycle_core/retire.py
```

Now that the verb exists, the honest state is a live
`kind_grew_without_exit` FINDING — 69 done bodies, never compacted.

**`audit` MOVING FROM TWO FINDINGS TO THREE IS THE POINT, NOT A
REGRESSION.** Any arm or baseline pinning "audit exits 3 with TWO
findings" is re-examined ONE BY ONE against this change, never
bulk-updated. MUST-NOT-MOVE: the other two findings keep their exact
text, and a repo with no done bodies still reports the kind clean
rather than firing.

---

## LANE 2 — `opus-lc146`

Trailer: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`

Write set: `plugin/cli/lifecycle_core/cli.py`, `test/test_verbs.py`

### lc-146 — a destructive default behind a carrier-verb fallthrough

`_carrier_verb` ends in an unguarded `return
verbs.cmd_item_close(...)`. Find both:

```bash
grep -n "_carrier_verb" plugin/cli/lifecycle_core/cli.py
grep -n "cmd_item_close" plugin/cli/lifecycle_core/cli.py
```

It is safe today only because the call site's action tuple gates
which actions reach it — so an action ADDED to that tuple without
its own branch silently runs CLOSE, a destructive two-file MOVE,
under another verb's name. The safety lives in the CALLER while the
risk lives in the callee, and a future edit to the tuple carries a
destructive default nobody reading the tuple would see.

Settled: the fallthrough cannot run CLOSE for an unrecognised
action. Either every action dispatches from an explicit branch and
an unknown one RAISES or answers could-not-verify, or the
fallthrough names the single action it serves and refuses anything
else.

Red-first, constructible without touching the caller: drive
`_carrier_verb` with an action string in no branch and assert it
does NOT reach `cmd_item_close` — today it does.

MUST-NOT-MOVE: every action that legitimately dispatches today still
reaches the same verb, **asserted PER ACTION rather than in
aggregate** — an aggregate arm passes on a refuse-everything build.

**This lane edits the same function last wave's compaction verb
added a branch to.** Read that branch before you change the
dispatch shape; it is in your file and not in your item.

---

## LANE 3 — `opus-lc120`

Trailer: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`

Write set: `plugin/cli/lifecycle_core/verbs.py`,
`plugin/cli/lifecycle_core/refusals.py`, `test/test_moves.py`

### lc-120 — a closed body gains a forward pointer, append-only

A closed body's slots cannot be corrected, so any that turn out
FALSE keep asserting themselves forever. `item amend` refuses a
closed body and **that refusal is RIGHT** — the done home holds what
was true when the item closed, and editing it there would rewrite a
record other things cite.

**THE DESIGN IS RULED IN THE ITEM: APPEND ONLY.** A new verb appends
`closure-superseded-by: <date> <ledger-ref> <one line>` to the moved
body, and the done-home check accepts that line as well-formed. The
original `closed-reason` is never touched. Read the item's full raw
block for the rest — it carries more than this summary.

Your write set already includes `refusals.py` because a new verb
that refuses anything needs its registry row (law 2, law 24). If the
verb emits a new `FINDING [...]`, register it there with a
fire/control differing in ONE dimension.

**If you record a prove-rows arrangement, it is admitted on a PAIR,
not on a PROVEN** — this repo's prose since last wave: the real
anchor giving `rows changed: <your row>`, and the same entry
re-pointed at an INERT anchor giving `rows changed: NONE` and a
FAIL. **BOTH HALVES of the inert probe must be inert** — the desk
re-pointed an anchor at a comment while leaving executable
replacement text, broke the module, and darkened nearly the whole
roster. FAILED either way; only one of them is a negative control.

---

## TAIL — binds every lane

A mid-run message may not arrive before the turn ends: on a gap
HALT THE ITEM, FINISH THE REMAINDER, REPORT — never halt the LANE.
Closing report (mandatory; "none" is a valid slot answer, silence
is not): (a) items completed w/ evidence, (b) checks RUN w/ real
output — FULL counts incl. skips, each dispositioned; a skip in a
check YOU built is a finding, (c) gaps surfaced — incl. anything
needing a tier above yours, returned as a question with evidence,
(d) deviations w/ reason, (e) candidate lessons, (f) files touched
+ commit hashes (unpushed), claimed by CONTENT not by trailer,
(g) what was NOT verified, (h) sources actually read.
Every claim about something OUTSIDE your own work names the read
that opened it, or carries "inferred, unverified".
Drain your inbox before sending and between parts.
Message ≤3000 chars each; split into labeled parts (1/N) — do NOT
write a report FILE. A missing decision, file, or value is
surfaced as a gap, never bridged with a guess.
A backgrounded check is AWAITED before the closing report; a report
sent with one still running is an INTERIM report and says so.
Commits unpushed, by pathspec — `git commit -m "…" -- <paths>`,
every flag BEFORE the `--`. Trailer: the one in YOUR lane section.
Never amend — always a new commit.
After sending the report your write grant is over: a defect you
find later is REPORTED, never edited.
