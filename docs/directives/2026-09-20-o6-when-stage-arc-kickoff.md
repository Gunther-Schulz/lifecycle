# O6 design arc — the WHEN/reader-trigger stage. Kickoff, 2026-09-20

Operator GO 2026-09-20 (answering the driving desk's numbered round:
"open it now"). Delegated peer desk: **lifecycle-ef** (opus). Judgment
holder for design: lifecycle-ef, within this scope. Grading, booking,
release gate, and the operator interface: the driving desk
**lifecycle-d9**. The delegation is inert until the operator states it
first-hand in lifecycle-ef's session; wave authorization waits for that
acknowledgment.

## Scope

Design the WHEN/reader-trigger stage — the-loop.md's O6 gap. The design
answers, for the reader/trigger side of the kind schema, what law 26
demands: what must be WRITTEN at each firing moment, whose absence is
computable, and — before that — where a DEFAULT makes the writing
unnecessary. Sign-off requires the transition table (arrow → verb →
record → check → OBSERVER) naming its own home, per the repo CLAUDE.md.

## Grounding — read at the artifact, never from this file

This file carries POINTERS only; paraphrasing them here is the drift the
corpus names. Read in the repo at HEAD:

- `docs/the-loop.md` — the O6 row and its neighbours (O8, I10 are
  deliberately parked; do not scope-creep into them).
- `docs/answerable-not-felt.md` AND `docs/answerable-not-felt-research.md`
  (the research companion CORRECTS the design doc's central framing; the
  design doc's own header demands it).
- `docs/purpose.md` — the kill test.
- `docs/directives/2026-09-19-answerable-arc-decision-round.md`,
  addendum 5 — the standing agenda this arc serves.
- Both probe results (new since the agenda):
  `docs/audits/2026-09-19-erosion-probe-design.md` (THE PROBE HAS RUN —
  HEALTH clears the instrument-accumulation objection) and
  `docs/audits/2026-09-20-direction-drift-trigger-probe-design.md`
  (baseline run; trigger-sufficiency COULD NOT VERIFY).
- Repo `CLAUDE.md` laws 23, 24, 26; and one live specimen of the
  trigger-stage vocabulary gap: the fire-log kind's declaration
  (`.claude/lifecycle.json`, kind "the fire log") declares
  `trigger: none` with its stated reason — the trigger is the verb
  LAYER and `verb <name>` cannot spell that.

## Boundary and gates

- NO writes to the shared lifecycle checkout — design artifacts live in
  your own scratchpad; distillates travel by SendMessage to
  lifecycle-d9; realizing writes and bookings land at the driving desk.
- GATED ON THE TREATMENT ARM, explicitly: the goal-question SLOT
  admission is NOT this arc's to decide — it waits on
  `docs/directives/2026-09-20-drift-treatment-arm-directive.md`'s
  outcome. Design the stage; leave that slot's admission marked open.
- Cadence: one interim when the scope reading is done and the design
  question set stands; then batched digests; immediate messages for
  blockers and decisions only. Decisions travel to the operator through
  lifecycle-d9.

## Deliverable

A design proposal for the WHEN stage plus its decision-round material —
numbered options with a recommendation each, every state-dependent claim
carrying its basis — sized for the standing decision round, not a build.
