# Drain wave 4 — 2026-09-15

Four lanes, disjoint by measurement. Authorized by the judgment desk
(dotfiles-89) on the cut below; dispatched by the drain desk.

The join: `item waves --grouped` reports ONE lane over 43 path-valued items,
which is true and useless — a single `test/` DIRECTORY entry written by nine
items covers files named by 33 others and bridges the set transitively. The
scheduling answer is the ITEM-level join over the subset actually scheduled.
Pairwise intersection of the five candidates' parsed write-sets returned
exactly one collision (lc-149 × lc-151, on both their files) and no directory
entries. So: four lanes, parallel.

---

## COMMON — binds every lane

**BASE.** Your dispatch message names the base commit. Verify it as a
CONTENT check, never as ancestry:

    git diff --quiet <base> HEAD -- <your write-set paths> && echo SAFE

Ancestry (`merge-base --is-ancestor`) is the wrong instrument here: this
desk has rewritten unpushed history once this week to drop a leaking
commit, which orphans a base hash while changing nothing in your paths. If
the content check passes, proceed whatever HEAD has become; if it fails,
STOP and report.

**READ FIRST, in this order.**
1. The `dispatch-guards:executor` skill. Conduct, under-report principle
   and report discipline live there and are not restated here.
2. This repo's `CLAUDE.md` — the laws, and the `## Verify` block whose
   commands are the battery you run. **Read law 24 AT HEAD**, not from your
   session-start injection: it was rewritten this wave to predicate
   altitude and your snapshot may predate it.
3. Your item BOTH WAYS: `lifecycle item slots <id>` for resolved truth, and
   the raw block in `ITEMS.md` for its amendment history. The amend lines
   are not decoration — every lane this wave has an amended slot, and the
   resolved view drops the reasons.

**WRITE BOUNDARY.** Exactly the paths in your item's `write-set:` slot,
nothing else. Two rules learned the hard way this week:
- A CARRIER AMENDMENT IS NOT A GRANT. If the desk amends your item's
  write-set, that is testimony that the ITEM widened — it does not widen
  your box. Only a message addressed to you does.
- If you need a path outside the set, STOP AND REPORT with the measurement
  that shows you need it. Do not reach. A lane last wave did exactly this
  and was granted one line within minutes.

**COMMIT.** By pathspec, every flag BEFORE the `--`:
`git commit -m "..." -- <paths>`. Never `git add` then commit; never `-A`.
For a NEW file, `git add -N <path>` first, naming a FILE and never a
directory. NEVER amend. NEVER push — the push is the desk's after
verification. ONE commit per item unless your item's own text says
otherwise.

**DO NOT TOUCH THE CARRIERS.** `ITEMS.md`, `ITEMS-DONE.md`, `LEDGER.md`,
`JOURNAL.md` are the desk's. It books every closure. If your criterion
seems to require editing one, that is a CONTRADICTION IN THE ITEM — report
it rather than resolving it silently. (This happened last wave; the lane
substituted a synthetic fixture and said so, which was right.)

**THE BATTERY.** Whole battery, every time. No `-k`. No `-x`. Both narrow
the measurement silently: `-k` grades the cases you already suspected, `-x`
leaves every later case UNRUN and reporting identically to green. Answer
with the failure LIST, never a count.

**READ THE failures=/errors= SPLIT PER ARM, off the RESULT OBJECT.** An arm
that reds as an ERROR proves only that the code is NEW — a fixture reaching
through a name the old side lacks errors before any assertion runs, and
scores identically against a build that carries the name and does nothing.
Only an assertion FAILURE at the defect counts as a red. Do not read a
console rendering for which test did what; use `res.failures` / `res.errors`
/ `t.id()`.

**RED-FIRST, three branches — pick by what you are proving.**
- NEW behaviour → run the new expectations against the OLD build, in a
  private clone at your base.
- Proving an EXISTING fix → the pre-fix commit is the old side.
- Pinning an ALREADY-TRUE property → there is no red against the current
  build, and that is not a licence to ship it unproven: TWO mutations, one
  of the BUILD it grades and one of its own ARRANGEMENT, both reds being
  assertion FAILURES.
State the BASELINE result too: over an already-red baseline a
mutate-and-revert proof is indistinguishable from a check that is always
red.

**THE CLONE'S OWN SELF-CHECK MUST BE GREEN FIRST.** A red from a broken
arrangement is indistinguishable from a red at the defect. Run the clone's
battery unmutated before you trust any red out of it.

**`tools/prove-rows.py` NEVER RUNS ON THE SHARED COPY.** It mutates tracked
files. Private clone only, and its verdict is then about the COMMIT, not
about the working tree. This copy has live co-writers.

**RESTORING A DELIBERATELY MODIFIED FILE.** Never `git checkout --`, `git
restore`, or `git stash` — all three are whole-file or whole-tree
destructive against uncommitted work, and this copy is shared. Restore from
a copy you took BEFORE the modification, and confirm by sha256 against
`git show HEAD:<path>` — the committed blob, which cannot lie. `git status`
is corroboration, never the verdict, and it needs `git update-index
--refresh` (sometimes twice) to be worth reading at all.

**MUTATION HYGIENE.** `PYTHONDONTWRITEBYTECODE=1` plus a `__pycache__`
purge per run. Two same-size mutations inside one mtime second are
indistinguishable to the bytecode cache and the second arm parrots the
first. PRINT the changed line back off disk after mutating: a mutation that
never applied returns a green byte-identical to a real one.

**READ YOUR OWN DELETION SIDE before anything runs**, and report it:
`git diff -U0 <paths> | grep '^-'`. Only lines you meant to change may
appear. AND CLASSIFY THEM — a list of four called "all intended" produces
the number without the property the number is for. Say which are additive
extensions and which CHANGE AN EXISTING EXPECTATION; the second kind is
what this read exists to catch.

**TWO ARRANGEMENT-DEPENDENT FIGURES, so you do not report a clone artifact
as a finding.**
- `lifecycle audit` in a CLONE reports extra `kind_grew_without_exit`
  findings, because `read_fire_log` filters by repo path and a clone matches
  none of this repo's recorded exit events. The live-tree figure is 2
  findings. Report the clone number as a clone number or run it live
  (audit is read-only).
- The unittest battery shows `skipped=1` in a clone and `skipped=0` in the
  real checkout — one arm reads a sibling repo that only exists here. Both
  are correct.

**REPORT.** `SendMessage` to `main`. (A send to `dotfiles-f1` is refused
from inside this process as its own session name.) Closing report per the
executor skill's form; IMMEDIATE message for a blocker, a decision, or a
milestone — do not sit on a blocker until the close. Scratch goes in YOUR
OWN scratchpad, never another session's.

**IF YOUR MEASUREMENT CONTRADICTS YOUR ITEM'S EVIDENCE SLOT, SAY SO.** The
slots were written before lanes landed and at least one has already gone
stale by a sibling lane's commit. Reporting a contradiction is the work,
not a complaint.

---

## LANE A — lc-149 + lc-151, ONE lane

`plugin/cli/lifecycle_core/retire.py`, `test/test_retire.py`

Both items are the same function's defect and are bundled deliberately:
`list_home` decides a home's notion, and it is wrong in two directions.
lc-149: an EMPTY carrier (head only, no blocks) has no heading, falls to
the plain-file branch and counts as ONE instance — so a kind whose home
exists and is empty fires `kind_grew_without_exit`, while the same home
ABSENT counts 0 and reports clean. lc-151: a home that is not
repo-relative can never resolve, because `list_home` builds `repo / home`
— and the miss is a silent count of 0 that the growth check reports as
CLEAN. Two of the registry's 21 kinds are blind that way, and one of them
is a fire log of over a million lines.

Fixing one and not the other leaves the notion-of-home question
half-answered inside one branch, which is why this is one lane and one
commit is acceptable here if the two repairs share a hunk — say which you
did.

**Do not add an arm asserting the current miscount.** A test pinning the
defect enshrines it.

## LANE B — lc-150

`plugin/cli/lifecycle_core/migrate.py`, `plugin/cli/lifecycle_core/refusals.py`,
`test/test_migrate.py`

`migration_ledger_nonzero` counts the ledger's TOTAL lines rather than the
lines a run routed, so every repo that has ever recorded a decision gets a
FINDING from any `--merge` while the merge still writes. Law 11 shape: a
guard firing on legitimate work trains the reader to discount the red that
will one day be real.

Both halves must be proven: the false-fire arm goes FINDING → CLEAN on your
fix, and the true-fire arm must NOT move. Note the consequence recorded in
the item — while this over-fires, `migrate --merge` end to end is
unverified, because the finding masks the run's exit code.

## LANE C — lc-153

`CLAUDE.md`, `tools/git-hooks/pre-push`

The repo documents its leak scan as armed via `tools/git-hooks/pre-push`
symlinked into `.git/hooks/`. The symlink exists exactly as documented and
git never consults it: `core.hooksPath` is set, and it OVERRIDES
`.git/hooks` entirely. The scan does run — through a foreign hook that
invokes it — so nothing has leaked, but the repo's stated mechanism is not
the one protecting it.

**THE DESIGN IS RULED (a) — do not re-open it.** Document the mechanism
that actually executes; do NOT attempt to make this repo's own hook
reachable. Git consults exactly one hooks directory, so that is not
implementable from inside this repo. The cross-repo fix is booked in the
dotfiles repo as df-254 and your text points there.

Your verifier is a PAIR, and the second arm is the one that matters: the
scan running under the configured hooksPath, and the answer with
`core.hooksPath` unset. A check asserting only that the symlink exists is
the exact defect being repaired and may not be the verifier.

## LANE D — lc-154

`tools/write-set-dependents.py`, `test/test_tools.py` (both NEW —
`git add -N` each FILE before committing)

Law 24 now says a write boundary is complete only once every change
resolves to what REALIZES it AND to every file that must MOVE WITH it. The
second half has no observer. For ONE shape it is computable: where a change
edits a source-derivable ENUMERATION, the files that derive it and grade
themselves against it are greppable before a lane is briefed.

**PROVE ON A PAIR DRAWN FROM THIS REPO, never on a planted row.** Known
positive: `cli.py`'s `item_action` tuple, whose answer MUST contain
`test/test_verbs.py` — the file whose absence cost lc-120 a halt. Known
negative: an enumeration no file grades, whose answer must be empty. A tool
returning `test_verbs.py` for every input has not discriminated.

**THREE ANSWERS, not two.** An enumeration it cannot resolve answers COULD
NOT VERIFY and NAMES what it could not read. It must never return an empty
list there: an empty list reads as "nothing depends on this", which is the
exact false clean this tool exists to prevent.

**Claim nothing wider.** One instance, one measurement — the lane that
found the shape declined to call it a class and that grade travels. A
general dependents checker over item prose is judgment-shaped and would
fire on legitimate work (law 11).
